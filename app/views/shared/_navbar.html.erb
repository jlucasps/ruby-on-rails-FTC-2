<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Nome do Projeto', index_path, class: 'brand' %>
      <div class="nav-collapse collapse">
        <p class="navbar-text pull-right">
          <% if user_signed_in? %>

            <ul class="nav pull-right">
              <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome, <%= current_user.name %> <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li>
                    <%= link_to "<i class='icon-cog'></i>Editar conta".html_safe,
                                edit_user_registration_path  %>
                  </li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to "<i class='icon-off'></i> Logout".html_safe, 
                                destroy_user_session_path, method: :delete %>
                  </li>
                </ul>
              </li>
            </ul>

          <% else %>
            <%= link_to 'faça login', new_user_session_path, class: 'btn' %>
          <% end %>
        </p>
        <ul class="nav">
          <li class="active"><%= link_to 'Início', index_path %></li>
          <li><%= link_to 'Sobre', about_path %></li>
        </ul>
      </div>
    </div>
  </div>
</div>